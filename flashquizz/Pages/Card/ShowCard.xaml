<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="flashquizz.Pages.Card.ShowCard"
             xmlns:models="clr-namespace:flashquizz.Models"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="80,*">

        <!-- nav custom -->
        <Grid Grid.Row="0" HeightRequest="80">

            <BoxView Color="Black" />
            <Image Source="navd.png" Aspect="AspectFit" HeightRequest="80"/>
            <Image Source="orc.gif" 
                HeightRequest="80" 
                Aspect="AspectFit" 
                IsAnimationPlaying="True"
                Margin="300,0,0,0"/>

            <Label 
             TextColor="#FFD700"
             FontSize="24"
             FontAttributes="Bold"
             HorizontalOptions="Center"
             VerticalOptions="Center"/>

            <ImageButton
             Source="backarrow.png"
             HeightRequest="80"
             WidthRequest="80"
             Margin="15,0"
             HorizontalOptions="Start"
             VerticalOptions="Center"
             Clicked="OnBackClicked"/>
        </Grid>

        <!-- contenu de la page -->
        <Grid Grid.Row="1">
            <BoxView Color="Black" />

            <Image Source="ba1.jfif" Aspect="AspectFill"/>

            <!-- Overlay dégradé en haut seulement -->
            <BoxView HeightRequest="200" VerticalOptions="Start">
                <BoxView.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <!-- noir complet en haut -->
                        <GradientStop Color="#FF000000" Offset="0.0" />
                        <!-- transparent en bas du BoxView -->
                        <GradientStop Color="#00000000" Offset="1.0" />
                    </LinearGradientBrush>
                </BoxView.Background>
            </BoxView>
            <StackLayout Padding="20" Spacing="20">
                <Label Text="{Binding Title}"
                     FontSize="32"
                     FontAttributes="Bold"
                     LineBreakMode="WordWrap"
                     HorizontalOptions="Center"
                     TextColor="#FFD700"
                     FontFamily="OrbitronBold"
                     HorizontalTextAlignment="Center">
                    <Label.Shadow>
                        <Shadow Brush="#000000" Opacity="0.9" Offset="3,3" Radius="6"/>
                    </Label.Shadow>
                </Label>
                <Button
                     x:Name="CardAddBtn"
                     Text="+"
                     Clicked="OnClickedAddCard"
                     BackgroundColor="#2A2A2A"
                     TextColor="#FFD700"
                     FontSize="20"
                     HeightRequest="50"
                     WidthRequest="50"
                     CornerRadius="80"
                     BorderColor="#FFD700"
                     BorderWidth="3"
                     HorizontalOptions="Center"
                     Opacity="0.8">
                    <Button.Shadow>
                        <Shadow Brush="#000000" Opacity="0.8" Offset="4,4" Radius="12"/>
                    </Button.Shadow>
                </Button>

                <CollectionView ItemsSource="{Binding Cards}" SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Card">
                            <Frame BackgroundColor="#2A2A2A" BorderColor="#FFD700" CornerRadius="8" Padding="15" Margin="0,5" Opacity="0.85" InputTransparent="true">
                                <Label Text="{Binding Question}" TextColor="#FFD700" FontSize="20" FontAttributes="Bold"/>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </Grid>
    </Grid>
</ContentPage>